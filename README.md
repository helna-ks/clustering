# clustering
This project uses the Iris dataset from Scikit-learn to perform clustering using K-Means and Hierarchical Clustering algorithms. The data is preprocessed and standardized before applying both algorithms. The resulting clusters are visualized using scatter plots and a dendrogram to understand the grouping of different Iris flowers.
